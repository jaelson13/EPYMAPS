<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.educacao.epymaps">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppThemeMenu">
        <activity
            android:name=".Activitys.TelaLogin"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activitys.TelaCadastro"
            android:theme="@style/AppTheme" />
        <activity android:name=".Activitys.HomeActivity"
            android:theme="@style/AppTheme"

            />
        <activity android:name=".Activitys.TelaRecuperarConta"
            android:theme="@style/AppTheme"
            ></activity>
    </application>

</manifest>